# 실제 값은 .env 에 두고 커밋하지 않는다.
# 두 키가 모두 없으면 서버는 목 모드로 뜨고, /forge 응답 형태는 동일하다.
#
# 스탯과 이미지는 제공자가 다르다. Gemini 스탯은 무료 티어에서 잘 돌지만
# Gemini 이미지 모델은 무료 할당이 0이어서(실측) 쓸 수 없었다.
# 한쪽 키만 넣어도 그쪽만 동작하고, 없는 쪽은 폴백으로 처리된다.

# ── 스탯 (그림 + 설명 → 이름·공격력·연사·탄속·사거리) ──
# https://aistudio.google.com/apikey — 무료 티어로 충분하다
GEMINI_API_KEY=
GEMINI_STATS_MODEL=gemini-flash-latest

# ── 무기 그림 1·2단계 (image-to-image) ──
# https://platform.openai.com/api-keys — 신규 계정 크레딧으로 시험 가능
# 무료 티어는 없다. 1024x1024 한 장당 대략 $0.011~0.167.
OPENAI_API_KEY=
# gpt-image-1 은 2026-10-23 지원 종료 예정이라 쓰지 않는다
OPENAI_IMAGE_MODEL=gpt-image-1.5

# 키가 있어도 목으로 강제하고 싶을 때
FORGE_MODE=

PORT=8790
HOST=127.0.0.1
# 이미지 생성이 느릴 수 있어 넉넉히
FORGE_TIMEOUT_S=180
